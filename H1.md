# Homework 1: An Introduction to Image Processing Using Python
For this homework, refer to the tutorials given in [my AIML GitHub repo](https://github.com/pattichis/AIML).<br>
Unless otherwise stated, you can assume that the homework refers to 8-bit grayscale images (e.g., problem 1).
For full credit, you must submit:<br>
 A. Screenshots of images involved.<br>
 B. Python code. PDFs are fine.<br>
 C. Discussion is always required for all parts. Often, a single sentence is enough. You need to comment on what your images represent.<br>

# Problem 1. Basic Image Representations (20 points)
Use the [interactive image editor](http://ivpcl.unm.edu/ivpclpages/Research/aolme/app/interactive-img-v2.html) to generate:<br>
 A. A binary image.<br>
 B. A greyscale image.<br>
 C. A color image.<br>

# Problem 2. Contrast of image patches (20 points)
For this problem, refer to [example of using image patches](https://colab.research.google.com/github/pattichis/AIML/blob/main/Session_8_Create_videos.ipynb#scrollTo=vhSh4F_an5j4).<br>
  A. Create an example with a minimum (non-zero) contrast for 8-bit grayscale images.<br>
  B. Create an 8-bit grayscale image example with maximum contrast.<br>
  C. Create an image with zero background and constant patches of 5, 10, 100, 200, and 250.<br>
  D. Apply the logarithmic transformation $\log(1 + C\cdot I)$ for different values of $C$ and discuss the visibility of the different patches.<br
  
# Problem 3. Images through activation functions (20 points)
For this problem, refer to the [image patches example](https://colab.research.google.com/github/pattichis/AIML/blob/main/Session_8_Create_videos.ipynb#scrollTo=vhSh4F_an5j4)
and the [activation functions tutorial](https://github.com/pattichis/AIML/blob/main/Session_14_Activation_Functions.ipynb).<br>
   A. Create an image with zero background and the constant patches of 5 and 100.<br>
   B. Show how a ReLU with proper bias will eliminate the lower-brightness patch.<br>
   C. Show how the use of a Sigmoid with proper bias will reduce the lower-brightness patch.<br>

# Problem 4. Max Pooling and downsampling (40 points)
For this problem, refer to the [max pooling example](https://github.com/pattichis/AIML/blob/main/Session_13_Max_Pooling.ipynb).<br>
Also, note that for grayscale morphology, dilation is equivalent to max pooling without downsampling.<br>
   A. Create an image with zero background, a rectangular patch with a value of 100 with a smaller rectangular hole.<br>
   B. Use the rolling-ball analogy from dilation to estimate the output from dilating with a 2x2. Show your work using pencil and paper.<br>
   C. Verify your answer using max pooling.<br>
   D. Demonstrate how you can increase the size of the max pooling to eliminate the smaller rectangular hole. 
      What is the minimum square size of your kernel? Derive a mathematical expression.<br>
   E. Suppose that the hole still appears after the max pooling operation. Sketch a diagram to show how it can disappear during the downsampling process.
   F. Suppose that the input image is multiplied by -1. Then apply the max pooling operation without downsampling. What is the relationship of the output image
      to applying an erosion to the input image? 
   G. How can you implement erosion using max pooling and multiplication by a constant?


# Problem 5. Image segmentation using histograms (40 points)
For this problem, use [image analysis session](https://github.com/pattichis/AIML/blob/main/Session_7_2_image_analysis_with_color.ipynb).<br>                       
Adjust the thresholds to detect:<br>
   A. The bear<br>
   B. Skin color (yellow-like), and<br>
   C. Clothes for the minion.<br>
You will need to provide screenshots for each case that show the histograms, Input, Binary Image, and
Output.

# Problem 6. Video creation (40 points)
For this problem, use the tutorial given in [video creation tutorial](https://github.com/pattichis/AIML/blob/main/Session_8_Create_videos.ipynb).
and [video template](https://github.com/pattichis/AIML/blob/main/Session_8_Video_template.ipynb).
Construct a fun video that satisfies the following requirements:<br>
   A. Uses NumPy arrays and uses at least 10 frames and a maximum of 30 frames.<br>
   B. You have two small objects moving in the video.<br>
Submit the final video and a printout of the PDF of the Jupyter notebook that you used to create it.   
